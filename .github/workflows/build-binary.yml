name: Build Linux Binary

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-22.04
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller
          pip install -e .
          
      - name: Create entry point
        run: |
          echo '#!/usr/bin/env python3' > bbot_entry.py
          echo 'from bbot.cli import main' >> bbot_entry.py
          echo 'if __name__ == "__main__":' >> bbot_entry.py
          echo '    main()' >> bbot_entry.py
          cat bbot_entry.py
          
      - name: Build binary
        run: |
          python -m PyInstaller --onefile \
            --name bbot \
            --collect-all bbot \
            --hidden-import bbot \
            --hidden-import bbot.cli \
            --hidden-import ansible_runner \
            ./bbot_entry.py
            
      - name: Test binary
        run: |
          chmod +x dist/bbot
          ./dist/bbot --help
          
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: bbot-linux-amd64
          path: dist/bbot
          
      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: dist/bbot
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
